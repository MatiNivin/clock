# Sudoku Generator

This repository includes a Python script for generating Sudoku puzzles. The `sudoku_generator.py` file creates a 9x9 Sudoku board and removes a configurable number of cells to produce a playable puzzle.

## Usage

```bash
python3 sudoku_generator.py
```

The script prints the generated puzzle to standard output using `.` to denote empty cells.
