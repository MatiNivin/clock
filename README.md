# Sudoku Generator

This repository includes a JavaScript script for generating Sudoku puzzles. The `sudoku_generator.js` file creates a 9x9 Sudoku board and removes a configurable number of cells to produce a playable puzzle.

## Usage

```bash
node sudoku_generator.js
```

The script prints the generated puzzle to standard output using `.` to denote empty cells.
